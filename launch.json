{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Local Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "envFile": "${workspaceFolder}/.env",
            "program": "${workspaceFolder}/cmd/server/main.go"
            // "program": "${workspaceFolder}/cmd/db/seed.go"
        }
    ]
}
